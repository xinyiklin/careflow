export const SIDEBAR_COLLAPSED_WIDTH = 64;
export const SIDEBAR_EXPANDED_WIDTH = 196;
export const NAVBAR_HEIGHT = 64;

// Shared horizontal page gutter. The navbar header and the main content frame
// both consume this so their left/right edges stay aligned — change it here to
// adjust page spacing in one place.
export const PAGE_GUTTER_X = "px-2 sm:px-3 lg:px-4 xl:px-5";
